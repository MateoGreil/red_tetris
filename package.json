{
  "name": "red_tetris",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm install && npm start",
    "server": "cd server && npm install && npm start",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "test": "./node_modules/mocha/bin/mocha --require babel-core/register --reporter spec"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4"
  },
  "dependencies": {
    "concurrently": "^4.1.1"
  }
}
